# This file makes 'backend' a Python package.
# WHY: Python needs this file to recognize the folder as an importable package.
# Without it, "from backend.api import main" would fail with ModuleNotFoundError.
